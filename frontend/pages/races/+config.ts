import type { Config } from 'vike/types'

export default {
    title: 'F1 Race Calendar | PitLane',
    description: 'Full Formula 1 race calendar — schedules, circuits, session times, and upcoming Grand Prix information.',
} satisfies Config
