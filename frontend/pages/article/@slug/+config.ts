import type { Config } from 'vike/types'

export default {
    // Title and description are overridden at runtime via useConfig() in +Page.tsx
    title: 'PitLane F1',
    description: 'Formula 1 news and analysis from PitLane.',
} satisfies Config
